import styled from 'styled-components';

export const PrinciplesStyles = styled.section`
  background-color: rgb(var(--blue) / 5%);
  padding: var(--padding);
  h2 {
    color: rgb(var(--blue));
    margin-top: 0;
  }
  ul {
    list-style: none;
    padding-left: 0;
    li {
      color: rgb(var(--dark));
      margin: 1rem 0;
    }
  }
`;
